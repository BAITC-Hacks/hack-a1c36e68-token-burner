[pytest]
testpaths = tests
addopts = -m "not integration"
filterwarnings =
    ignore::DeprecationWarning:importlib._bootstrap
markers =
    integration: slow end-to-end run on data/samples with a real LLM (opt-in: pytest -m integration)
